{
  "name": "rspack-esbuild-diff",
  "version": "1.0.0",
  "description": "different between rspack and esbuild about artifact",
  "main": "index.js",
  "scripts": {
    "build:r": "rspack -c rspack.config.js",
    "build:e": "node build.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@rspack/cli": "^1.0.14",
    "@rspack/core": "^1.0.14",
    "esbuild": "^0.24.0"
  }
}
